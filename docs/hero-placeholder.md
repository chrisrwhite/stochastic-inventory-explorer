# Hero screenshot placeholder

The README links to `docs/hero-placeholder.png`. Replace it with an actual
screenshot of the deployed app (Set up page or Results page works well) and a
short demo GIF. The current file is intentionally missing so it is obvious
this is unfinished.

Suggested captures:

- `hero-setup.png` - Setup page mid-flow, with demand history + lead-time
  previews visible.
- `hero-results.png` - Results page on the "Cost vs reliability" tab with the
  recommended dot highlighted.
- `demo.gif` - 5-10 seconds, tweaking the reliability slider and watching the
  frontier update.
